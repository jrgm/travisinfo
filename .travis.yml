language: node_js

node_js:
 - "0.10"

sudo: false

notifications:
  email:
    - jrgm@mozilla.com

before_install:
  - ls -la .

before_script:
 - uname -a
 - cat /etc/lsb-release || echo 'no /etc/lsb-release'
 - echo foo
 - echo bar
 - echo baz
